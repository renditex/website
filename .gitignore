# Claude Code lokale Einstellungen und Skill-Tools — nicht Teil der Website
.claude/

# OS
Thumbs.db
.DS_Store

# Node (nur fuer die Netlify-Functions in netlify/functions/ benoetigt).
# package-lock.json bewusst NICHT ignoriert — sorgt fuer reproduzierbare
# Netlify-Builds.
node_modules/

# Netlify CLI lokaler Zustand
.netlify/
deno.lock

# Lokale Testgeheimnisse fuer "netlify dev" — niemals committen
.env
.env.*
